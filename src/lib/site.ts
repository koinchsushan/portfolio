/**
 * The one place the site's deployed origin is written down. Every relative
 * OG/canonical URL on the site resolves against this via `metadataBase`
 * (`src/app/layout.tsx`), so moving to a custom domain later is a one-line
 * change here rather than a search-and-replace across every route's
 * metadata.
 */
export const SITE_URL = 'https://sushansunuwar.vercel.app'
